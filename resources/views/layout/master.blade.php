@yield('header')
@yield('slider')
  <!-- Start Promo section -->
  <section id="aa-promo">
    <div class="container">
      <div class="row">
        @yield('content')
      </div>
    </div>
  </section>
  <!-- / Subscribe section -->

  <!-- footer -->  
 @yield('footer')